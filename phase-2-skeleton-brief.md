# بریف فاز ۲ — اسکلت‌سازی وب‌سایت deVino

این فایل را همراه با `CLAUDE.md` (که تصمیمات کلی برند و استک فنی در آن است) به Claude Code بده. هدف این فاز فقط ساخت اسکلت فنی سایت است — بدون جلوه‌های بصری نهایی، بدون افکت زوم هدر، بدون محتوای واقعی. آن‌ها در فازهای بعدی اضافه می‌شوند.

## دستور برای Claude Code

راه‌اندازی یک پروژه‌ی جدید Next.js (App Router) با TypeScript و Tailwind CSS برای وب‌سایت deVino. لطفاً مراحل زیر را دقیقاً و به‌ترتیب انجام بده:

### ۱. راه‌اندازی پروژه
- ایجاد پروژه‌ی Next.js جدید با App Router، TypeScript، Tailwind CSS
- اتصال Sanity به‌عنوان CMS (schema اولیه در بخش ۴ همین فایل مشخص شده)
- تنظیم ESLint و Prettier با پیکربندی استاندارد

### ۲. تنظیم توکن‌های طراحی (Design Tokens) در Tailwind
رنگ‌ها و فونت‌های زیر را دقیقاً طبق `CLAUDE.md` در `tailwind.config` تعریف کن:
- `matte-black`: `#000000`
- `pearl-white`: `#F8F6F0`
- `olive-accent`: `#556B2F` (فقط برای استفاده‌ی بسیار محدود — کامنت بگذار که این رنگ نباید در پس‌زمینه‌ی بخش‌های بزرگ استفاده شود)
- فونت هدینگ: Cormorant Garamond (از Google Fonts لود شود)
- فونت بدنه: Inter (از Google Fonts لود شود)

### ۳. ساختار صفحات (Routing Skeleton)
هر صفحه فقط باید یک هدینگ ساده و متن placeholder داشته باشد در این مرحله — بدون طراحی نهایی:
- `/` — صفحه‌ی اصلی
- `/collections` — لیست کالکشن‌ها
- `/collections/[slug]` — صفحه‌ی یک کالکشن خاص (لیست محصولات آن کالکشن)
- `/products/[slug]` — صفحه‌ی جزئیات یک محصول
- `/about` — درباره‌ی برند
- `/contact` — تماس / رزرو
- Layout مشترک شامل هدر (فقط لوگوی متنی DEVINO فعلاً) و فوتر ساده (لینک‌های شبکه‌ی اجتماعی placeholder + فرم خبرنامه‌ی ساده)

### ۴. تعریف Schema های اولیه‌ی Sanity
- **Product**: نام، اسلاگ، توضیحات، آرایه‌ای از تصاویر (هرکدام با فیلد hotspot/focal point)، قیمت (عدد)، موجودی (عدد یا boolean)، کالکشن مرتبط (reference)
- **Collection**: نام، اسلاگ، توضیحات، تصویر کاور (با hotspot)، آرایه‌ای از محصولات مرتبط
- **MediaBox** (به‌عنوان یک object قابل استفاده‌ی مجدد در schema های دیگر، طبق تعریف در `CLAUDE.md`): فیلد `type` (image/video)، فیلد `asset`، فیلد `focalPoint`
- **SiteSettings**: یک سند singleton برای اطلاعات عمومی سایت (نام برند، تگ‌لاین، لینک‌های شبکه‌ی اجتماعی، اطلاعات تماس)

### ۵. کامپوننت MediaBox (فقط ساختار، بدون افکت نهایی)
کامپوننت React برای نمایش تصویر/ویدیو از Sanity بساز که:
- داده را از schema بالا می‌گیرد
- در این فاز فقط تصویر یا ویدیو را به‌درستی و ریسپانسیو نمایش می‌دهد (بدون افکت زوم — آن در فاز ۳ اضافه می‌شود)
- ساختار کد باید طوری باشد که افزودن افکت‌ها در آینده به یک کامپوننت متمرکز، بدون تغییر در بقیه‌ی سایت ممکن باشد

### ۶. آماده‌سازی برای استقرار
- پروژه باید بدون خطا روی Vercel (Preview Deployment) قابل بالا آمدن باشد
- یک لینک preview در پایان کار ارائه شود

## چیزی که در این فاز **نباید** انجام شود
- افکت زوم روی هدر (Ken Burns) — فاز ۳
- تغییر رنگ هنگام اسکرول — فاز ۳
- طراحی بصری نهایی گرید محصولات — فاز ۴
- سبد خرید یا هرگونه منطق پرداخت — طبق `CLAUDE.md`، این فاز جداگانه‌ی آینده است، ولی فیلدهای قیمت/موجودی از همین الان در schema محصول وجود دارد

## خروجی مورد انتظار از Claude Code
یک پروژه‌ی قابل اجرا، با تمام صفحات بالا (حتی خالی)، متصل به Sanity، با توکن‌های رنگی/فونت تعریف‌شده، و یک لینک preview روی Vercel که بتوان آن را در مرورگر باز کرد و مسیرهای مختلف را دید.
