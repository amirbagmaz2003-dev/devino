# بریف فاز ۳ — هدر نهایی، افکت زوم، گذر رنگ اسکرول، لوگو

این فاز روی صفحه‌ی اصلی (Hero/هدر) کار می‌کند. اسکلت فاز ۲ را به یک تجربه‌ی بصری واقعی تبدیل کن — پایه‌ی کار قبلاً در `CLAUDE.md` تعریف شده (بخش‌های «معماری کامپوننت مدیا»، «رفتار اسکرول هدر»، «لوگو»)؛ این فایل فقط جزئیات اجرایی همان تصمیمات را مشخص می‌کند.

## ۱. افکت زوم روی تصویر هدر (Ken Burns)
- از کامپوننت `MediaBox` موجود استفاده کن (نه یک کامپوننت جدید)
- وقتی `type === "image"`: هنگام لود شدن صفحه، تصویر با `scale(1)` شروع می‌شود و طی حدود ۸ تا ۱۰ ثانیه با `ease-in-out` به `scale(1.3)` می‌رسد، حول `focalPoint` تعریف‌شده در Sanity
- افکت باید یک‌بار در لود اجرا شود؛ نیازی به تکرار حلقه‌ای (loop) نیست
- تصویر placeholder فعلی: هر عکس مناسب evening-wear با مجوز تجاری از Unsplash/Pexels کافی است (موقت است، بعداً از پنل ادمین عوض می‌شود) — یک `focalPoint` منطقی (مثلاً روی صورت یا بالاتنه‌ی مدل) برایش تنظیم کن

## ۲. گذر رنگ هنگام اسکرول
- هنگام اسکرول از هدر به بخش بعدی، رنگ پس‌زمینه باید مستقیماً با موقعیت اسکرول (scroll-linked، نه timer-based) از `#000000` به `#F8F6F0` تغییر کند
- این تغییر باید کاملاً نرم باشد (میان‌یابی رنگ فریم‌به‌فریم، نه پرش بین چند رنگ ثابت)
- از یک scroll listener بهینه استفاده کن (مثلاً با `requestAnimationFrame` یا کتابخانه‌ای مثل Framer Motion's `useScroll`) تا روی موبایل هم روان باشد

## ۳. لوگو
- دو فایل لوگو (`devino-logo-black-transparent.png` و `devino-logo-white-transparent.png`) در پروژه ضمیمه شده‌اند؛ در `public/` یا از طریق Sanity SiteSettings قرار بده
- لوگوی متنی موقت `DEVINO` در هدر و فوتر را با این فایل‌ها جایگزین کن
- در هدر: همزمان با گذر رنگ پس‌زمینه (بخش ۲)، لوگو باید بین نسخه‌ی سفید (روی پس‌زمینه‌ی تیره) و نسخه‌ی مشکی (روی پس‌زمینه‌ی روشن) با یک کراس‌فید کوتاه (حدود ۳۰۰ تا ۵۰۰ میلی‌ثانیه) جابه‌جا شود، هماهنگ با نقطه‌ای از اسکرول که کنتراست پس‌زمینه دیگر کافی نیست
- در فوتر (که همیشه روی پس‌زمینه‌ی روشن‌تر است): فقط نسخه‌ی مشکی لوگو استفاده شود

## ۴. تست
- روی موبایل (که اکثریت بازدیدکننده‌ها احتمالاً از آن استفاده می‌کنند) رفتار اسکرول و افکت زوم را حتماً تست کن، نه فقط دسکتاپ
- مطمئن شو افکت‌ها در حالت راست‌به‌چپ (فارسی) و چپ‌به‌راست (انگلیسی) هر دو درست کار می‌کنند
- بررسی کن Lighthouse performance score افت محسوسی نکرده باشد (افکت‌های بصری سنگین می‌توانند سرعت را کاهش دهند)

## چیزی که در این فاز نباید انجام شود
- طراحی نهایی بخش گرید محصولات/کالکشن‌ها — فاز ۴
- محتوای واقعی (عکس‌های عکاسی‌شده) — تا اطلاع بعدی از placeholder استفاده شود

## خروجی مورد انتظار
لینک preview به‌روزشده (همان `devino.amirbagmaz2003.workers.dev` یا یک preview جدید) که در آن بشود هدر را دید: زوم آرام روی عکس، اسکرول با گذر رنگ نرم، و لوگوی واقعی که هماهنگ با اسکرول رنگ عوض می‌کند.
